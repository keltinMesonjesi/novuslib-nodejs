/*
 |--------------------------------------------------------------------------
 | Autoloaded Models
 |--------------------------------------------------------------------------
 |
 | The models listed here will be automatically loaded on the
 | request to your application. Feel free to add new models to
 | this array in order to utilize them.
 | *The given name for the model must match the name in the define function.
 |
 */

const models = [
  'User',
  'UserDetail'
]

module.exports = {
  models
}
